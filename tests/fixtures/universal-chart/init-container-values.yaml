image:
  repository: ghcr.io/example/app
  tag: "1.2.3"
initContainers:
  - image: amazon/aws-cli:latest
    command:
      - s3
      - sync
      - "--delete"
      - "--no-progress"
      - "s3://{{ .s3_bucket }}/{{ .number }}"
      - /app
