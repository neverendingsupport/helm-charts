apiVersion: v1
kind: Service
metadata:
  name: {{ include "dbinstance.fullname" . }}
  labels:
    {{- include "dbinstance.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "dbinstance.selectorLabels" . | nindent 4 }}
