apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "dbinstance.fullname" . }}-test-connection"
  labels:
    {{- include "dbinstance.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "dbinstance.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
