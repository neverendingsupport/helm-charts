apiVersion: backstage.io/v1alpha1
kind: Component
metadata:
  name: helm-charts-ack-elasticache-provider
  title: ack-elasticache-provider
  description: Helm chart for ACK-managed ElastiCache replication groups and exported connection secrets.
  annotations:
    github.com/project-slug: neverendingsupport/helm-charts
    backstage.io/techdocs-ref: dir:.
  tags:
    - helm
    - kubernetes
    - aws
    - redis
    - elasticache
    - infrastructure
spec:
  type: library
  lifecycle: production
  owner: group:default/infrastructure
