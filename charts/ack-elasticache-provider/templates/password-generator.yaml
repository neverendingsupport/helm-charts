{{- if and .Values.connectionSecret.create (eq .Values.auth.mode "password") (not .Values.sequencedConnection.enabled) }}
apiVersion: generators.external-secrets.io/v1alpha1
kind: Password
metadata:
  name: {{ include "ack-elasticache-provider.passwordGeneratorName" . }}
  labels:
    {{- include "ack-elasticache-provider.labels" . | nindent 4 }}
spec:
  length: {{ .Values.auth.password.length }}
  digits: {{ .Values.auth.password.digits }}
  symbols: {{ .Values.auth.password.symbols }}
  noUpper: {{ .Values.auth.password.noUpper }}
  allowRepeat: {{ .Values.auth.password.allowRepeat }}
---
apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: {{ include "ack-elasticache-provider.externalSecretName" . }}
  labels:
    {{- include "ack-elasticache-provider.labels" . | nindent 4 }}
spec:
  refreshInterval: 1h
  target:
    name: {{ include "ack-elasticache-provider.connectionSecretName" . }}
    creationPolicy: Merge
    template:
      type: Opaque
      data:
        {{ .Values.connectionSecret.keys.username }}: {{ .Values.auth.username | quote }}
        {{ .Values.connectionSecret.keys.port }}: {{ default 6379 .Values.port | quote }}
        {{ .Values.connectionSecret.keys.tls }}: {{ (.Values.transitEncryptionEnabled | default false) | quote }}
        {{ .Values.connectionSecret.keys.url }}: ""
        {{ .Values.connectionSecret.keys.authURL }}: ""
  data:
    - secretKey: {{ .Values.auth.password.key }}
      sourceRef:
        generatorRef:
          apiVersion: generators.external-secrets.io/v1alpha1
          kind: Password
          name: {{ include "ack-elasticache-provider.passwordGeneratorName" . }}
{{- end }}
