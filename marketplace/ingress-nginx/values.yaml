# AWS Marketplace distribution overlay.
#
# CI merges these values into a staged copy's values.yaml before packaging.
# Every rendered image must resolve to an AWS Marketplace-managed ECR
# repository so AWS can replicate the product across regions.
controller:
  image:
    chroot: false
    registry: 709825985650.dkr.ecr.us-east-1.amazonaws.com
    image: herodevs/ingress-nginx-nes
    # Marketplace ECR tags are immutable and undeletable. The bare
    # v1.15.1-nes-1.15.7 tag there predates the OCI-identity relabeling and
    # fails the AWS scan with a false-positive CVE mapped from upstream
    # labels; -2 is the clean respin the published preview version uses.
    tag: v1.15.1-nes-1.15.7-2
    digest: ""
    digestChroot: ""
  admissionWebhooks:
    patch:
      image:
        registry: 709825985650.dkr.ecr.us-east-1.amazonaws.com
        image: herodevs/ingress-nginx-nes-kube-webhook-certgen
        digest: ""

defaultBackend:
  image:
    registry: 709825985650.dkr.ecr.us-east-1.amazonaws.com
    image: herodevs/ingress-nginx-nes-defaultbackend
